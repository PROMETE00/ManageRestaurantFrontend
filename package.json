{
  "name": "restaurante-workspace",
  "private": true,
  "workspaces": [
    "admin-web",
    "customer-web",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=admin-web --workspace=customer-web",
    "lint": "npm run lint --workspace=admin-web --workspace=customer-web"
  }
}
